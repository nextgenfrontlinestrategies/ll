{
  "name": "locallink",
  "version": "1.0.0",
  "private": true,
  "description": "LocalLink backend and frontend for location-driven local discovery",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.0",
    "express": "^4.19.0"
  }
}
